# ui-next 壳布局规范(Layout Contract)

> 布局先行:功能只能放进本规范的格子里。改格子=改本文件+对应实现,不许就地发明。

## 0. 壳形态换代(2026-08-18 用户终案:**工作台即主界面**)

应用启动即工作台形态,旧「rail + 侧栏 + 单会话主区」三列壳**退役**:
- 导航全部收进工作台**任务列的双 tab**(本地项目/云端任务,tab 无图标
  nowrap、**tabs-box 形态 + 轨道补 base-300/40 底**(轨道在 base-200
  侧栏上同色隐形、白 pill 孤悬,2026-08-19 报障「太丑」后显形=macOS
  分段控件;创建页坐白卡不用补)——与创建页 kind 页签同语汇,白 pill=
  选中填充;tabs-border 的通栏下划线在双 tab 下孤线感重,2026-08-18 定案;
  tab 短名「本地/云端」(2026-08-19 mockup)。同日 mockup 小件:待办组头
  **未完成计数徽标**(badge-ghost,有则显)、项目组前**「项目」小节帽**
  (xs 12px 帽层,跟第一颗真项目走——临时会话可拖进项目之间)、组头**开合
  chevron 殿后常驻**(状态外显)、**设置图标+文字沉左下**。mockup 的
  「选中行左竖条」**不跟**(与待办 attention 行首条撞通道,填充只归选中
  的定案不动);「更多项目 ⊕」待定义,未做。tab 上方是**品牌行**(Brand 字标+work 徽标,旧侧栏头
  同款,自带拖拽区);列表行回 **menu 默认档 14px**——menu-sm 是 12px,
  踩了四档字阶的空档,2026-08-18 用户报障「字体大小不对」;「本地会话」tab 同日撤并——chat 无项目可归,收进本地列表成
  「临时会话」组:与待办同构的哨兵组(\0chats 入 mc.collapsedGroups),
  组头常驻 + hover「+」快捷新建会话,组内嵌「已归档会话」小节沿用
  mc.archivedOpen——形态与项目组「已归档任务」小节同构(Archive 头 L1、
  行 L2;SectionFold 是底部通栏小节形态,进组内不带缩进,2026-08-18
  用户报障「缩进不太对」后换手写头);**与项目组同一条 mc.projectOrder 快照参与拖动排序**
  (哨兵键入序,默认位 = 待办之下、项目组之前;插入点按「序里 chats
  之前仍存活的项目数」折算,项目来去不破位——2026-08-18 用户定案);无标题无计数行——2026-08-18 用户连番定案。任务列是
  **整窗高左列**:顶部 chrome 行(mac 灯净空 + 列开关)、中段 tab+列表、
  底部 更新提醒条(旧侧栏 UpdateFooter 原样回迁)+ 设置行——参考图
  重排与 2026-08-20 补遗);首启缺省**单格**(无存档树;旧缺省 2col 的
  空右栏对新用户冷场,2026-08-20 定案);行政事务(行右键 改名/归档/删除)、待办组、**项目全套**
  (组头右键 在此新建/归档项目、组头拖拽排序 mc.projectOrder、项目内
  「已归档任务」小节 mc.sessionArchivesOpen、底部「已归档项目」小节
  mc.projectArchiveOpen——2026-08-18 报障后按旧侧栏对表补齐,分组必须
  吃全量含归档,先滤再分组小节恒空;同日二次报障「完全不对」再对表:
  组头图标 Folder/FolderOpen 随开合、**裸项目名**(工作台消歧特例撤销)、
  waiting 徽标、hover「+」快捷新建,并撤「运行中」置顶区)、云端列表(CloudTaskList 整体
  复用)随之迁入任务列。
- **右侧顶条只在「列收起」时存在**(☰ 开关、「新建」兜底钮、mac 灯净空
  之家)。列开着时右侧无任何顶条——**平铺分栏终案**(2026-08-19 用户
  mockup,当日浮卡随之退役;格形态三代:灰底白卡 → 同底浮卡 → 平铺):
  格 = **白底通栏、1px 细线分隔**(grid 底色透缝;浮卡的 12px 缝在多格
  时每格白吃 ~30px 宽),侧栏灰/内容白两分、侧栏右缘线回归;**焦点格 =
  细头标题 primary 下划线**(只垫文字宽,tab 同语汇;ring 焦点环退役,
  落点 secondary 虚环保留);细头白底(淡底分块退役)且空白区自任拖窗面
  (§7 属性不继承,标题的换位拖拽/按钮不受扰)。
  布局模板钮整线退役(四连 join→下拉→四连三代后 2026-08-18 用户终案
  「没啥用」:拆分/关闭本身就是布局手段);「新建」主钮的家在任务列顶行。**新建即新格**(2026-08-18
  用户定案「创建任务也是一个 panel」):列侧一切新建入口(列顶主钮/组头
  「+」/组头右键)有空格先用空格,没有就把焦点格向右拆一格**专供创建**
  (spawned 标记,取消即收回不留尾巴),不设格数上限;格内入口
  (提示卡/装载卡)仍就地创建。**窗顶 chrome 行(列顶行/
  列收起顶条/设置页头)一律 h-10(40px)+ mac 4px 底衬**——先按 mac 原生灯(垂直中心 ≈18)定过 h-9
  同心,用户复看「有点太小」加到 40;「行心 20 差 2px 无感」是错判,灯是
  大圆点真机一眼即穿(同日二次报障),app.css 给 mac 的窗顶 chrome 行
  统一 padding-bottom:4px,内容行心压回 18 与灯严格同心(2026-08-18;
  设置页头同高同衬;格细头/格内创建页头升 h-12 主头,见 §4);
  左列顶行与主区头**同高同底色、无分隔线**,拼成一体的 L 形 chrome,
  内容画布是**独立的一块**(深半档 bg-base-300/70 + 左上圆角内嵌,
  用户定案「sidebar 和 header 做一个整体,内容区域单独一块」)。
  **设置齿轮沉在任务列底部**(2026-08-18 用户定案「放在 sidebar 的下方」;
  列收起时经 ☰ 一击可达,头部不留第二个门)。覆盖视图只剩设置一种
  (优先级 设置 > 工作台):**整页新建视图已退役**(2026-08-18 用户定案
  「那个页面没用了」)——创建只有格内一种形态,NewTaskModal 的 embedded
  变体升为唯一形态(logo 居中回归、标语不回;h-9 页头;页签 =
  **本地任务/云端任务双 tab**——「本地会话」并入本地任务,差异只在目录
  选择器的「临时会话(不选文件夹)」档:空目录即建 chat,「目录必填」
  拦截随之退役,2026-08-18 用户定案「只是是否选择项目」;云端建成装
  "c:" 条目进本格),待办派发经 createRequest 意图通道送进工作台走
  「新建即新格」,todoId 回链不丢。
- 云端任务与本地一样**入格**(CloudTaskView pane 变体;槽位条目经
  slots.ts 的 "c:" 记号分流)。
- 原生窗口标题跟随焦点格。mc.lastSession(旧壳契约键)只读一次用于
  首开播种,升级不丢「上次看的那个」。
- 下文 §1.3 三列结构、§2 rail 角落格、§6 旧侧栏四段式为**退役留档**
  (历史语汇与教训仍适用;listKit 行/组头/小节件与待办组规范继续在
  工作台任务列生效)。工作台本体的规范见 §4「工作台」条目。

## 1. 层级(自底向上)【§1.3 起为退役留档,见 §0】
1. **窗体 chrome**:Windows/Linux = **28px** 全宽窗框条(**条内只有右端 caption
   三键**,其余是拖拽区);mac = **原生红绿灯留系统默认位**(x≈7 起、
   垂直中心 ≈18;壳走 Overlay,**挪灯 API 实测无效**——tauri 2.11.5 的
   traffic_light_position 在 Overlay 下灯纹丝不动,真机截图为证,对齐
   反着做:UI 窗顶 chrome 行一律 h-10 + mac 4px 底衬,内容行心压回 18 与灯同心)。UI 只做让位——**占住
   左上角的那条 chrome** 显式挂 data-mac-lights-clear(任务列在场 = 列顶
   chrome 行,收起 = 主区视图头,覆盖视图 = 各自页头),app.css 无层区
   统一 76px 左净空,**不画条**;
   自绘替身 2026-08-18 随「工作台即主壳」退役,原生灯的全屏隐现/失焦退灰/
   长按缩放菜单不用再仿);浏览器 = 无。
   平台判据只有一个:`isCustomChromeShell()`(host.ts),别在组件里散写。
   > **不追求三端结构相同**(2026-08-08 定案):右边缘只有一条,让窗控与视图动作
   > 去抢,谁赢另一个都会被挤到中间当孤儿;mac 没这问题只因它的窗控在左边。
   > 该统一的是设计语言,结构随平台——mac 没有标题栏条是因为 macOS 窗口就没有。
   > 「把 caption 并进 52px 头带」的方案**已废弃,别再提**;视图动作三端一律
   > 留在主区头部最右缘。

   窗框条的三条铁律:
   - **不放品牌/视图信息**。曾摆过一份品牌字标,与紧挨其下的侧栏头凑成上下
     两行同样字样(2026-08-07 用户报障「两个 header」);后来又在左端摆过品牌
     标记(为的是别让窗口左上角空一块),2026-08-09 一并挪去 rail 角落格(见 §2)
     ——条内从此只剩 caption 三键与拖拽区。
     系统菜单改由**条上右键**唤起:那是标题栏的东西,挂到侧栏图标上会变成
     「双击侧栏图标把应用关了」的陷阱。
   - **不承列色**(2026-08-08 定案)。删掉品牌文字还不够:条按 rail/side/main
     复刻三列配色时,`base-200` 色块 y=0..36 紧贴着又一个 `base-200` 侧栏头,
     **那才是「两个 header」的真正成因**。当年保留分段是为了"不断色"——但
     窗框本来就该跟内容断开,为了不断色而让它假装成内容的延伸,恰恰是它冒充
     header 的原因。整条单色(`bg-base-300`),列宽令牌不许出现在条里。
   - **不带底边线**。有线就成了 header 基线。

   > 2026-08-20 修订:铁律拦的是品牌/列色/底线这些「冒充 header」的东西,
   > **不拦功能 chrome**——任务列收起时,工作台的 ⟨☰⟩/新建两颗 ghost 钮
   > 借住条左端(App 经 TitleBar `leading` 提供寄宿位,SplitView portal
   > 进驻;用户报障「折叠后为两颗钮单开一行 h-10,空一行好丑」)。列展开
   > 时双钮回列内品牌行,条回归纯 chrome。

   条高 28px:低于 Win11 caption 的标准 32(用户两次反馈顶部太厚,2026-08-09)。
   **caption 宽度仍守 46px 系统度量**——横向才是决定「点不点得中」的那一维
   (三键并排,错一格点成邻居),纵向瘦只是少占地方。改条高必须同步 app.css
   的 `--chrome-h`,chromeHeight.test.ts 会对表。

   Linux 一并走 CSD(壳侧 `decorations(false)`):保留原生装饰栏的话,三端里
   唯一不受控的那端最显眼。代价是 WM 的 resize 边/右键标题菜单/部分平铺手势
   没了——resize 由 UI 侧边缘热区补(§8.1),右键菜单由 `window_system_menu`
   命令补(Windows;GTK 侧无对等 API)。
2. **全局横幅**:EngineBanner(引擎生命周期专用),chrome 之下、三列之上,全宽。
3. **三列内容**:rail(w-rail,bg-base-300)/ 侧栏(w-side,bg-base-200,border-e)/
   主区(flex-1,bg-base-100)。
   **登记在案的唯一例外——工作台(全窗沉浸)**:SplitView 在场时 rail 与
   侧栏都不渲染(2026-08-16 用户定案「rails 有点多余」,推翻同日早先
   「rail 保留」版),视图独占窗口;退出口 = 工作台视图头 ✕;mac 红绿灯
   移居工作台头部左端(rail 角落的家没了,窗控不能跟着消失)。设置/新建
   在场时三列 chrome 回归。设计与理由:
   docs/superpowers/specs/2026-08-16-desktop-split-view-design.md。
4. **浮层**:菜单/模态/抽屉,z 序见 z-index 约定(backdrop<pop<drawer<lightbox<toast)。
5. **角落瞬态**:后台会话提醒 toast、全局下载 dock(右下)。

## 2. 头部基线
- 三列各自长出 **h-13(52px)** 头部,同高对齐成一条贯通线(border-b border-base-300);
  两行文字(标题+副标题)在 44px 里太挤,52px 是定稿高度(2026-08-04)。
- **rail 角落格恒存在**(与三列头部同高,基线才对得齐),里面按平台放不同东西:
  mac = 红绿灯的家(壳走 Overlay,窗控归 UI 自绘);**其余平台 = 28px 品牌标记**
  (不可交互,整格可拖窗)。侧栏头 = 品牌名 + ＋新建(云端空间另有刷新钮);
  主区头 = 当前视图提供(欢迎页为空头带)。
  > 曾对 Windows 开特例不留这一格,让第一个空间图标顶上去占位:尺寸恰好凑得上
  > (size-11 + py-1 = 52px)所以没露馅,但三个图标整体比其余平台高一格,契约里
  > 也从没写过这条。2026-08-08 删除。留出格子后它在 Windows/Linux 上是空的
  > (那格存在的唯一理由本就是 mac 要放灯),空着一块深色方格既浪费又难看
  > (2026-08-09 用户报障),于是把窗框条左端那枚品牌标记挪了进来——两处空档
  > 一次填平,窗框条也回归「只做 chrome」。rail 列从此是 Discord/Slack 同款的
  > 「标记 → 导航 → 设置」(工作台入口在底部簇设置之上,
  > 不在此导航区——三轮定稿 2026-08-17:空间区第 4 钮 → 主区头视图动作 →
  > 底部簇;它是模式不是空间,见 §4 工作台)。
  > 标记 28px:明显大过 18px 的空间图标才读得出
  > 「这是标记不是第四个导航项」。
- 拖拽区分布在各头部空白处(data-tauri-drag-region;按钮与可双击标题除外)。
- **固定定位覆盖层的顶偏移一律读 `--chrome-h`**(app.css,按根节点
  `data-platform` 取 0 / 28px),不许各自手算平台偏移。FilesDrawer 抽屉、
  daisyUI `.modal`、角落 toast 都走这条。
  > 2026-08-08 根治:此前 FilesDrawer 写 `isWindowsShell() ? "top-9" : "top-0"`、
  > toast 另写死 `mt-13`——后者那个 52 是照 mac 算的,Windows 上基线在 84px、
  > 提醒却从 68px 起,z-50 压住主区头右侧的文件/⋯ 动作钮。同一笔账算两遍、
  > 错一遍,变量收口治的是这一**类**,不是那一个。

## 3. 信息安放规则(每类信息只有一个法定位置)
| 信息 | 法定位置 | 禁止出现在 |
|---|---|---|
| 视图身份(标题/上下文路径) | 主区头部左侧 | — |
| 视图动作(文件/终端/⋯) | 主区头部右侧,图标钮 | — |
| 引擎崩溃/启动失败 | EngineBanner 全局横幅 | 任何头部 |
| **会话连接状态/恢复失败** | **主区头部之下的内嵌条**(恢复即消) | **头部** |
| 轮次/消息级错误 | 消息流内系统条目 | 头部、横幅 |
| 运行态(思考中/执行中/停止) | composer 上方状态行 | 头部 |
| 上下文用量 % | composer 底部集群右端 | 头部 |
| 模型/思考档/权限模式 | composer 底部集群左端 | 头部 |
| 任务清单(plan) | composer 上方面板 | 消息流 |
| 后台会话提醒 | 角落 toast + 侧栏行 attention(状态点放大加光晕) | 头部、消息流 |
| **会话/任务状态** | **侧栏行尾状态点**(仅要紧态;词进 title/aria,见 §6) | 头部、独立列、行内文字 |
| 更新可用 | 侧栏底部条 + 设置·关于 | 头部 |
| 下载进度 | 右下 dock | 头部、消息流 |

## 4. 主区视图形态
- 会话视图:头部 → (连接条) → 消息流(居中 `.chat-measure`) → 面板/状态行 → composer。
  composer 区**无上边线**(2026-08-13 用户定案:输入卡自带边框已是分界,
  再压一条通栏线是双重描边;云端任务视图同款)。
  - **列宽是 `.chat-measure` 一条**(app.css 无层区):`min(64rem, max(48rem, 92%))`
    ——地板 768(窄窗与死宽时代一致)、中段跟主区连续变宽留 4% 呼吸边、天花板
    1024(正文行长上界)。消息流与 composer **必须共用它**,分头写会错开中线。
  - 卡片/代码/diff **不许自行突破这条列**:正文守窄、卡片探头 = 参差不齐的两条
    边(2026-08-10 议过并否掉)。要更宽就整列一起宽,即只动 64rem 那个天花板。
- 设置/新建任务:视图级头部(标题+关闭)+ 居中内容列(max-w-2xl / max-w-xl);
  Esc 离开;点任何导航即切走(视图切换永远响应)。
  **待办没有主区视图**(2026-08-12 用户二次定案「先不需要右侧的页面,只在
  sidebar 实现添加和其他操作」;当日早先的「侧栏钉住行入口 + 覆盖视图」与
  08-11 的「rail 开关 + 覆盖视图」两版都已废,演进三步留档:rail 开关 →
  侧栏入口行 → 清单本体进侧栏):待办是本地任务空间侧栏列表顶部的一个
  **组**(见 §6.2),添加/勾选/编辑/删除/派发全在组内完成;「派发成任务」
  仍开主区新建视图(预填正文与图片,todoId 回链)。渲染优先级:设置 >
  新建 > 工作台 > 云端任务 > 会话。
- 工作台(2026-08-16 用户定案沉浸形态,2026-08-18 升格为应用壳(§0),
  「入口/退出」概念随 rail 一并退役):**任务列 = 整窗高左列**(参考图
  重排 2026-08-18,主流分栏 chrome 同构;**宽度可拖**:右缘 8px 透明把手,
  184–420 钳制、双击回 232、落盘 mc.workbenchListWidth(2026-08-20 用户
  「给一个最小的宽度就行」):列顶 chrome 行承 mac 灯净空
  (§1.1)**仅 mac 渲染**(2026-08-20 用户报障「两个操作空了一行」——
  非 mac 无灯,这行就是空行挂两颗钮,整行省掉),⟨☰⟩列开关与「新建」
  ghost 方钮**双钮靠右**、新建收尾贴角(2026-08-18 用户定案「都 float
  到右边」;非 mac 双钮住品牌行行尾;新建曾 soft-primary 文字钮,
  同日报障「按钮丑」降噪——chrome 里不落大色块,品牌感只在图标色,
  kind 跟任务列当前 tab、**新建即新格**,§0),底部右下设置齿轮)+ **主区视图头**(无常驻按钮。布局模板钮**整线退役**
  (四连 join→下拉→四连三代,2026-08-18 用户终案「没啥用」——拆分/
  关闭即布局手段;单格融合态的拆分钮在头部,多格在格细头);列收起时
  ☰、「新建」兜底钮按平台落位——**有自绘标题栏的端(Windows/Linux)
  借住条左端**(TitleBar leading 寄宿位 + SplitView portal,2026-08-20
  用户报障「折叠后也空一行」:28px 条本来就在,再开一行只装两颗钮是
  纯浪费),mac/浏览器仍走主区头部左端补钮与净空标记;无标题字、
  **h-10 无底线**——与列顶行
  同高同底色拼 L 形一体 chrome(2026-08-18 定案,复看「太小」由 h-9 加高))+ **可折叠任务列**(w-side 左缘列,2026-08-17 定案治「光秃秃」:
  计数统计行与列内新建钮
  均撤(2026-08-18:前者「没啥用」,后者提级主区头部)+ **设置齿轮沉底**
  (border-t 小页脚右下,btn-ghost 方钮;§0)+
  本地 tab 组头**逐项对表旧侧栏**(2026-08-18 二次报障
  「完全不对」定稿:Folder/FolderOpen 随开合、裸项目名(消歧特例撤销,
  全路径进组头 title)、waiting 黄徽标、hover「+」快捷新建常驻占位、
  右键 在此新建/归档项目、拖拽热区挂 li + 落点线绝对定位;**无「运行中」
  置顶区**——行留组内,等待感 = 组头徽标 + 行尾状态点);项目组可点组头
  折叠(无箭头,折叠态共用 mc.collapsedGroups 与主侧栏同步);点行
  place 路由、拖行 LOAD_MIME 定点装载、已入格行带标记;**点格/焦点换人 →
  任务列联动**(tab 跟着切到条目所属空间——「云端 tab 点本地任务不切换」
  同日报障推翻「不硬切」初版;展开所在组 + scrollIntoView,data-row-id
  锚;revealTick 让同格重复点击也联动;2026-08-19 定案);云端行与本地行
  同款可拖(LOAD_MIME 定点装载,TaskRow 透传);mc.workbenchListHidden 记忆,缺省展开;任务列在场时空格退为
  轻提示卡)。**格区与其右栈父容器两层的
  min-w-0 是宽度总闸**(2026-08-18 溢出事故:缺一环整棵格区跟最宽内容
  走,类名机检钉在 SplitView.test)+ **平铺画布**(2026-08-19 mockup 终案,
  格形态三代:灰底白卡 → 同底浮卡 → 平铺:格白底通栏、1px 细线分隔
  (grid 底色透缝)、无衬无卡衣;**焦点格 = 细头标题 primary 下划线**
  (data-split-focus 迁至下划线,常驻透明底防抖),落点 secondary 虚环
  保留;**单格融合已反转退役**——格恒平铺、细头恒在,右侧顶条仅列收起
  时存在)+ **布局树迭代扁平渲染**(features/split/tree.ts,tmux 同构;树投影
  为 grid 下同级 pane/handle,深树不进入 JS/React/DOM 递归栈;用户终案
  「随便他搞」,固定档位模型退役):叶=格(不设数量
  上限)、内部节点=切分,每格细头可「向右/向下拆分」「关闭(兄弟上位,
  最后一格置灰)」,**按住格头标题拖拽**(HTML5 dnd 私有 MIME):落在
  Panel 中心交换位置；落在整个格区左/右边缘则搬成全局边列并收拢原位置，
  旧 Panel 宽度同比缩小、相对宽度不变，新列等于缩放后的最窄旧 Panel；
  落在目标 Panel 上/下边缘则搬入该 Panel 所在的连续纵向组，并把该组行
  等高。任务列拖行沿用四边语义新增 Panel，中心仍定点替换；中心落点用
  secondary 虚环，四边用对应边带高亮;
  每条分隔线恰是一个树节点,拖它只动两侧子树(固定档位时代"一条线牵动
  别的格"的三轮拉扯就此消解),8px 透明把手骑 1px 分隔线,树比例只守
  ε–(1−ε) 以容纳任意格数，手动拖动则按所属区域为两侧各留至少 48px；
  实际不足 48px 可用尺寸的深层子树仅保留焦点路径、不物化其余
  pane/handle，画布尺寸变化后重算；同屏 pane/handle 各有 512 DOM 物化
  预算（只限视图实例、不限制树中格数，焦点路径优先），避免 4K 画布上的
  合法深树仍扩成数万 DOM。双击平分;trackPointer 收尾纪律同
  FilesDrawer(mouseup 与卸载兜底双路
  收回 body 全局样式)。格 = 细头(**h-12 主头**——右侧无顶条后它就是每格
  的视图头,2026-08-19 用户「再大一点」;白底 + 拖窗面;状态点 12px 定宽
  槽/标题 14px(焦点下划线**绝对定位悬挂**,盒 block+w-fit——truncate 的
  inline-block 基线是盒底缘,行内基线对齐会把标题顶高 ~4px 离心,Chrome
  实测钉死;**双击就地改名**,⋯菜单同项,仅本地会话走 admin.onRename
  ——旧单会话头能力回归,2026-08-19 报障「不能从 header 修改标题」)/簇 =
  **[视图动作插槽][文件(本地)][⋯格操作][关闭]**——插槽是 display:contents
  的通用落点,视图经 createPortal 注入**可视钮**(本地投 文件、云端投
  文件/终端——**framework 无 files 特例**,本地文件抽屉也随之入格:
  FilesDrawer pane 变体 = 格内 absolute scrim+侧板,与云端 CloudFiles
  同构,「云端在格内、本地为啥全局」2026-08-19 用户报障;
  格头是唯一框架、不写任务类型分支,2026-08-19 用户定案「panel 都是
  通用的」;**视图菜单项经 provider 并入唯一 ⋯**——双 ⋯ 并排「怎么还有
  两个」同日报障,云端的 控制台/在线预览/终止/删除 开菜单时现取拼进
  格操作,confirm 两击走 openMenu 现成机制,端口预览取快照、开菜单触发
  拉取下次可见(动态分节只归整页头);插槽 ref 回调必须恒等,每渲染换新函数会 detach/attach 打状态
  直至 Maximum update depth,实测钉死);拆右/拆下/独占/更换四个分窗操作
  收进 ⋯ 菜单(openMenu,2026-08-18「不是常见的操作」);**关闭钮可关才渲染**(独格整颗不出,置灰版 2026-08-19「去掉
  吧」)。簇 hover 显隐(invisible 常驻占位),更换/独占态强制可见——
  **无**归档/删除,行政回任务列;空格也有细头,否则关不掉)+ ChatView
  pane 变体(无 52px 头/文件抽屉入口(细头代之);**大纲左缘浮轨格内
  照常**(2026-08-19 报障「大纲没了」解禁);composer **恒全套**——非焦点
  格轻输入条 2026-08-18 定过、2026-08-19 用户「不需要缩小」撤销,slim
  机器整套拆除;列宽 `chat-measure-pane`:同 64rem 天花板、无 48rem 地板);空格 =
  装载卡,**横向通栏 tab「本地项目(/云端任务)」**(tabs-box 均分列宽、
  无图标 nowrap,2026-08-18 定案;本地 tab = 运行中置顶(**只
  归装载卡**——快挑场景;任务列无此区)→ 项目分组——**分组喂全量任务再过滤
  行**,项目顺序与侧栏严格一致(2026-08-16 用户报障「排序跟 sidebar
  不一样」)→ 尾接「临时会话」平铺段(chat 撤并 2026-08-18;装载卡只管
  挑,新建会话走任务列组头「+」或创建页内切页签);组头用 listKit.GroupLabel 安静小标签 +
  组内行缩一级,**不用 menu-title**(主题下字号大过行,同日用户报障);
  列表区定高防切 tab 跳高,判重排除已入格);「新建任务/新建会话/云端任务」
  一律**格内创建**(NewTaskModal 唯一形态:根 div、无 hero、h-9 非拖拽
  页头、页签含云端;kind 跟随入口语境),整页新建已退役(§0)。**同一会话禁双格并存**
  (session_close 无引用计数,判重收口在 slots.assign);审批快捷键只归
  焦点格(shortcuts.ts enabled 参)。屏外任务 toast 照发,点击装格不切
  视图(App openSessionById 分屏分支)。
- 云端任务视图与会话视图同构。

## 5. 滚动纪律
- **列/视图级滚动容器只许纵滚**:凡 `overflow-y-auto` 必须搭 `overflow-x-hidden`
  (CSS 规则:只写 overflow-y 时 overflow-x 会被计算为 auto,超宽内容即出横向
  滚动条——侧栏横滚事故的根因)。超宽内容靠 truncate/min-w-0 链截断。
- **内容量可变的纵滚容器用 `[scrollbar-gutter:stable]` 预留滚条槽位**
  (2026-08-05 侧栏展开抖动的根因):chrome.css 自定义/标准化了滚条样式,
  内核即用经典布局型滚条(8px 挤占内容宽),auto 下滚条随折叠开合出现/
  消失,整列内容横移抖动。gutter 只预留空间不绘制,透容器底。
  **不用 `overflow-y-scroll` 常驻滚道**:壳内核走标准 scrollbar-width
  路径时忽略 ::-webkit-scrollbar 透明底,空滚道画成白条(同日用户报障,
  两轮定案);消息流居中列用 `stable both-edges` 对称留槽(中线对齐)。
- 横向滚动只允许出现在**专用滚动区**:代码块(pre)、diff/代码预览、markdown
  表格包裹层(.md-scroll)、xterm。

## 6. 侧栏内部结构(四段式;信息布局参考旧 UI,组件一律 daisyUI)
```
头部 h-13(固定):品牌 + ＋新建(云端加刷新)
概览块(固定,2026-08-04 用户定案):空间标题 + 一句描述 + 统计行
  (本地 = N 项目/N 任务,chat = N 对话,云端 = N 项目/N 任务;运行中
   primary/等待确认·排队中 warning 着色且仅 >0 时出现;云端 feed 由
   Sidebar 注入(useCloudTasks/useCloudProjects,enabled=仅云端空间),
   与列表同一份数据,首屏加载中不出统计行)
  **标题行恒留一个 btn-xs 的高度**:云端多一颗刷新钮(24px)而 text-xs 行盒只
  16px,不预留就是「有钮的空间比没钮的高 8px」,切空间时概览块往下跳
  (2026-08-09 用户报障)。高度写成与 btn-xs 同一个表达式(--size-field × 6),
  不写死——自定义主题里「控件尺寸」可调。同 §6.2 hover 显隐铁律:**行高不许由
  「这行恰好有没有那个元素」决定**。
列表(flex-1,唯一滚动区,纵滚横截):menu menu-sm + details 折叠
  (menu-sm 是实打实的档位:行 12px/行距 4px——曾漂移成裸 menu(14px/6px),
   2026-08-13 用户定案「标题字改小、行距改小」后对齐回来;组头与行同 12px,
   层级靠字重/降色与缩进,不靠字号)
  (本地任务空间列表顶部是「待办」**组**,§4,实现收口
   features/todo/TodoSection:项目组同款区块标签 + hover「+」开行内添加
   (Enter 连续记,粘贴截图随 Enter 挂上);行 = **纯文字安静行**(与会话
   行同款,行首无勾选件——checkbox-xs 与 14px 圆圈钮两版都试过,
   2026-08-12 用户定案「不需要前面的小圆点」,完成走右键首项「标记完成」)
   + 行尾图片角标(被动指示,数量进 tooltip)与要紧态状态点(派发去向
   回查会话表);**点行开详情弹窗**(2026-08-13 用户定案:「点击就是修改」
   不合直觉,行内编辑已废——弹窗承载正文编辑 + 图片管理(加/删/点大图/
   弹窗内粘贴)+ 派发与关联任务跳转,右键的「编辑/添加图片/查看图片」
   随之退役,菜单只剩 完成/派发/打开关联/删除);**未完成段行拖拽排序**
   (项目组同款落点线,顺序即 todos.json 数组序,2026-08-13 用户要求);
   「已完成」为组内小节折叠(mc.todoDoneOpen,默认收起);组折叠注册
   mc.collapsedGroups 哨兵 key \0todos。空态也保留——没有会话 ≠ 没有
   要记的事。未完成待办数进概览统计行(「N 待办」缀在任务数后,仅 >0
   时出现,用户定案 2026-08-12)。派发时图片预填进新建视图照旧)
footer(固定钉底):更新提示等常驻条,永不随列表滚动
```
> 搜索行已撤(用户指令 2026-08-04「搜索先去掉」);回归时恢复 input 行 +
> query 过滤 + 全折叠段强制展开(CloudTaskList 仍保留 query prop)。

### 6.1 行信息布局(用户定案 2026-08-04,二次定案「区块标签+安静行」;
载体 = menu 的 li>a)
- **行一律单行**:行首身份图标槽 + 主文案(**摘要优先**,缺席回落标题;
  标题进 tooltip)+ 行尾状态点殿后;云端行同构。**云端行尾点仅 error**
  (2026-08-06 用户定案):pending/processing 是云端任务的常态(VM 排队
  数分钟、执行数小时),常亮脉动点是噪音;运行/排队的一眼可见由概览
  统计行承担,状态词进行 tooltip。
- **安静行**:行首 12px 定宽槽固定显身份图标(本地 SquareTerminal /
  对话 MessageSquare / 云端 Cloud,/40;裸文字顶行首太秃,2026-08-05
  用户定案),**状态不顶掉身份图标**(2026-08-05 用户定案):要紧态 =
  行尾彩色状态点(等待确认 warning 脉动 / 运行中 primary 脉动 / 运行
  出错 error / 未读 attention warning),**文字状态词不上行**,进点的
  title/aria 与行 tooltip;静默态(N 轮/可继续/未开始/已完成)无点,
  轮次/终态词收进行 tooltip。**不展示时间**(用户定案)。
- **「填充」这个通道只归选中**(2026-08-10 用户定案):选中 = `menu-active`
  (primary 12% 淡填充);attention(D3 未读/等你处理)= **2px warning 竖条**
  (`listKit.ATTENTION_BAR`,绝对定位不挤布局)+ 行尾 warning 状态点。
  > 曾经两者都是整行淡填充(attention 走 `bg-warning/10`),只靠色相区分
  > ——而列表里「哪一行被填充了」本身就读作「这行是选中的」,于是屏幕上同时
  > 出现两个填充行,选中的那个就淹了(用户报障「本地任务需要操作时侧栏标题
  > 高亮,有点分不清哪个是选中的」)。**问题在通道重叠,不在颜色不够远**:
  > 色相拉得再开也还是两个"淡底行"在比谁更深。换边缘条后分工干净——填充
  > 只此一义,边缘条表示「这行在等你」,两者可叠加互不打架(主流树/列表
  > 组件与邮件客户端同款分工)。
  > 条子的 x **跟着本行缩进走**,落在「本行文字左缘 - 8px」(条宽 2px,留 6px
  > 呼吸),即引导竖线原先占的那条沟;级差表见 `listKit.LEVELS`,pad 与 bar
  > 成对定义,**不许拆开改**。初版钉在行左缘 `start-0`(图的是各层级待办行
  > 对齐同一条 x、一眼数得清),同日引导竖线一撤,x=0 那列空无一物,条子比
  > 项目组头的 Folder 图标还靠左、孤悬在内容列之外,读起来像贴在侧栏边框上的
  > 一道杂线(用户报障「最左侧的提醒条是不是太靠左了,感觉很奇怪」)。
  > **别再把 attention 改回整行底色。**
- **已归档行降到 `/55`**(2026-08-07 对表旧 UI `--t4` 定案):归档行此前
  用正文色,在列表里和活跃任务一样抢眼(用户报障「已归档的任务标题怎么
  还是黑色的」)。活跃行不覆写、走正文色;选中态不降档——选中就该看清,
  颜色交回 `menu-active-fg`。
  > 同批试过的「行降到 `/90`、组头满色加粗」两项已按用户定案回退,
  > 只留归档这一档。
- **行菜单 = 右键**(lib/contextMenu,menu 皮相):重命名(行内 input,
  Enter 提交/Esc·失焦取消)/归档/删除;删除与云端终止走 confirm 二段。
  行 tooltip 给 标题/摘要/目录/「右键管理」。

### 6.2 分组与折叠(daisyUI details 原生折叠)
- 项目组头 = details summary,**区块标签形态**(定案 2026-08-04 二次;
  2026-08-07 试过换成旧 UI 的「同字号 + font-semibold + 满色」锚点形态,
  用户定案回退,**别再提**):summary 用 flex 覆写默认 grid,
  项目图标(Folder 12px /40)+ 名称
  (text-xs font-medium /50;**保留原大小写**——目录名是标识符,
  不做 uppercase,用户定案 2026-08-04)flex-1 伸展 + waiting 徽标 +
  快捷「+」殿后(常驻占位 hover 显形);**原生折叠箭头去掉**(after:hidden,
  用户定案 2026-08-04,开合只靠点击组头);同名项目不做展示名消歧
  (「父 · 名」前缀已撤,用户定案 2026-08-04,靠 tooltip 完整路径区分);
  组头可右键(在此新建任务/归档项目);HTML5 拖拽排序,dragover 落点
  border-t 指示线。
- **层级靠缩进,不靠空白**(2026-08-07 用户四轮报障后定案:
  ①「亲密性不够,像一路排下来」②「项目之间太空了」③「看看别人类似组件
  怎么设计的」④「间距还是不对…看看之前 UI 是咋搞的」)。
  间距只留旧 UI 同款的组尾 6px(`pb-1.5`,组内小节 `pb-1`)。
  > ④ 之后曾按旧 UI 把组头改成「比行更重」的锚点(旧 UI 正是这么表达从属:
  > 组头 12.5px/600/`--t1`、行 12.5px/400/`--t2`),**用户定案回退**——组头
  > 维持安静小标签,层级不靠字重。留档以免再走一遍;归档行降色那一档保留
  > (§6.1)。
  **空白分组这条路是错的,别再走**:
  > 对表主流树组件——VS Code 资源管理器、Finder 列表视图、JetBrains 项目树、
  > GitHub 文件树、Notion 侧栏——**无一例外是等距行 + 零组间空白**,层级
  > 只由「缩进 + 折叠箭头 + 引导竖线」三样表达。用空白分组是 Slack/Linear
  > 那类**少数几个固定分区**(Channels / DMs)的手法:分区数是常量才成立;
  > 项目组数量随用户增长,每组多摊几十像素,列表立刻被撑散(②即此)。
  > 中间两版先后试过 `mt-1→mt-4` 常驻组间距、`open:mb-4` 展开态组间距,
  > 都是在错的维度上找平衡,已废弃。
  **引导竖线这一件也已撤**(用户定案 2026-08-10「本地会话项目列表的竖线
  都去掉,包括 archive 的列表」;带截图圈出 L1/L2 两条线)。08-07 曾按
  VS Code 同款给嵌套 `ul` 挂过 1px 淡竖线(`GUIDE_L1/GUIDE_L2`,
  `bg-base-content/15`,`start` 落在组头图标中心),现全部去掉——本地、
  对话、云端一并(§「三列表一套件」),层级只剩缩进 + 组头小标签。
  实现见 `listKit.NEST_NO_GUIDE`:**竖线本体是 daisyUI 自带的**
  `.menu :where(li ul,li menu):before`,「去掉」= 显式 `before:hidden`
  关掉它;类串一摘就退回 daisyUI 的默认线,不是冗余。
  > 三件套里的**折叠箭头这一件也不采纳**(2026-08-07 复核后用户维持
  > 08-04 原定案):对表时它是主流树组件的标配(可点击 affordance +
  > 折叠态信号),已带方案征询,用户选择不加——行宽优先。
  > **别再以「主流都有」为由提第三次。**
- **缩进阶梯进行内、行底满宽**(2026-08-05 用户定案,含截图纠偏;推翻
  08-04「组内不缩进」与同日两版 margin 缩进):嵌套 ul 一律拉平
  (`ms-0 ps-0`——margin 缩进会把 hover/选中底压窄且各级左缘错位,截图
  事故根因);缩进用行内起始 padding:**每级 +20px**(= 头图标 12 +
  gap-2 8):L1 行 `ps-8`、L2 行 `ps-13`(基准 item padding 12px)——
  每级**行文字与其父头文字同线**(组头文字 32 = L1 行、组内小节头文字
  52 = L2 行;2026-08-18 用户报障「好几条对齐线,好乱」:旧阶梯 +12 与
  头文字 +20 双增量并存,32/40、44/50 两对近失误全并进 20px 网格,任务列
  品牌行也落 20 图标线;小节头 10px 图标坐 12px 定宽槽,增量才恒 20);
  行首标记统一 12px 定宽槽(状态点/Archive/Folder 同列);层级链 = 项目头 → 任务行
  → 「已归档任务」小节头(Archive 图标行首、`after:hidden` 去尾箭头)→
  归档行;嵌套竖线**已去掉**(见上条,2026-08-10 定案);云端项目组同构
  (组 ul `[&>li>a]:ps-6`)。
- **折叠段内容收起即卸载**(条件渲染,不靠 details 原生隐藏):部分
  webview 里 details 收起后嵌套 ul 残留占位空间(2026-08-05 用户报障)。
- **三列表一套件,不做两套**(用户定案 2026-08-05,后续三空间并入同一
  tab 的横向双 tab):行/状态槽/区块标签/小节折叠收口在
  features/sidebar/listKit(ListRow/StatusSlot/GroupLabel/SectionFold),
  本地、对话、云端一律经它拼装,禁止各写一份行或组头。
- 归档/历史结构:项目内「已归档任务」小节;底部「已归档项目」;chat 底部
  「已归档会话」。云端与本地同构:进行中任务裸行置顶(同 chat 平铺行,
  **区标签 menu-title 已撤**)→ 项目组(Folder)→ 底部「历史任务」小节
  (History 图标)。「快速开始」组已撤(用户定案 2026-08-05:无项目的
  快速任务本就出现在进行中/历史里,分组视图是重复信息)。小节标签一律
  不带计数(用户定案 2026-08-05)。
- 折叠态持久化(旧 UI 契约键):mc.collapsedGroups / mc.sessionArchivesOpen
  (JSON string[])、mc.archivedOpen / mc.projectArchiveOpen /
  mc.cloudHistoryOpen("1"/"0",prefs.readFold/writeFold)。
- (搜索回归时)query 非空 → 全部折叠段强制展开、不写盘;云端未拉过的组
  顺势懒拉。
- menu 嵌套缩进竖线(li ul::before):**隐藏**(`listKit.NEST_NO_GUIDE`)。
  历经三轮:08-04 按用户指令隐藏 → 08-07 改成引导竖线(GUIDE_L1/L2)→
  **08-10 用户再次定案去掉,三列表一律无线**(见 §6.2 首条)。
- **hover 显隐铁律**:悬停才出现的元素不许插入布局(hidden→flex 会挤动
  同行内容形成抖动);只许 invisible→visible(常驻占位)或绝对定位。
- **menu 截断铁律**:daisyUI `.menu` 默认 `flex-flow: column wrap` + 自带
  nowrap——wrap 列的行宽跟内容走,truncate 不触发。**且 `.menu li` 自身
  也是 `column wrap`**(2026-08-04 溢出事故根因:只改顶层 ul 管不到行,
  嵌套行照样冲出行底)。列表级 menu 一律
  `w-full flex-nowrap [&_li]:flex-nowrap`,行内文字走 min-w-0+truncate 链。
  **浮层菜单(dropdown-content / contextMenu)同样适用**——定宽(w-64 等)
  替代 w-full,`[&_li]:flex-nowrap` 一个都不能少(2026-08-05 大纲面板
  时间被长摘要挤出即此因,行尾元素消失 = 先查这条)。

## 7. 拖拽区铁律(mac/Windows 自绘 chrome)
- Tauri 按**事件目标自身**是否带 `data-tauri-drag-region` 判定,**不继承**:
  头部容器带了没用,内部每个非交互子节点(标题 h1/副标题 p/占位 span/徽标)
  都必须单独带,否则子元素盖住的表面全部不可拖。
- 不带属性的例外:按钮等交互件、可双击重命名的文字 span(拖拽区双击会被
  吃成窗口最大化)。
- 新增头部时按本条自查;有 ChatView 结构测试可参照。

## 8. 阶段纪律:裸组件优先(用户指令,2026-08-04)
> 三层分治(同日教训):壳布局(本文件)/ 信息布局(可参考旧 UI)/ 组件
> 实现(一律 daisyUI 官方形态)——"参考旧 UI"只作用于中间层。
- 现阶段 daisyUI 组件一律用**官方文档形态**,不叠色彩/边线/阴影覆写
  (card 用 `card-border` 的 base-200 线;dropdown-content 用文档标准
  `menu bg-base-100 rounded-box shadow-sm`;激活态用 `menu-active/
  tab-active/btn-active` 原生类)。
- 统一视觉调整放到收尾,一次性经**主题变量**(monkeycode 主题块)完成,
  不散落在组件类上。
- 允许保留:功能性状态色(attention 行的 warning 行首条、状态点)、文字四档
  透明度(内容排版,非组件覆写)、布局结构线(列分界/头部基线)。
- **图标族 = `@tabler/icons-react`**(2026-08-07 由 lucide 换过来):组件名
  一律 `Icon` 前缀,线宽属性是 `stroke` 而非 `strokeWidth`(全库统一
  `stroke={1.75}`,组头 12px / 行内 13-14px / 空态 20px)。
  > 选图标两条:①**先确认是字形不是文字标记**——tabler 有一批画的是字母
  > (`IconJson` 就是「JSON」四个字),图集里清楚、12px 下糊成一团,必须
  > 按真实尺寸离屏出一版图再定;②**图标不能暗示不成立的前提**——如本地
  > 任务空间曾用 lucide 的 `FolderGit2`(带 git 分支的文件夹),而本地任务
  > 并不要求工作区是 git 仓库(FilesDrawer 专有非 git 降级分支),已换
  > `IconFolderCode`。

### 8.1 豁免清单(daisyUI 无对应形态,自绘保留;新增豁免须在此登记)
- TitleBar 的 Windows/Linux caption 三键:系统 chrome 规范(热区/配色/度量
  跟平台),btn 表达不了;§1 已归窗体 chrome 层(mac 红绿灯 2026-08-18 回
  原生,自绘豁免随之注销)。caption 键取
  **46×32 系统度量、直角通高触边**,与视图头部那排圆角内缩胶囊(btn-ghost
  btn-square btn-sm)形成形状对比——眼睛靠"贴不贴边"分组,不靠间距硬撑。
- ResizeEdges(Linux):窗口内侧 8 个透明拉伸热区(边 4px / 角 12px),
  `decorations(false)` 后 WM 的 resize 边没了,由它经 `start_resize_dragging`
  把拖拽交回壳。仅 Linux 渲染——Windows 的无边框 resize 由 tao 在 WM_NCHITTEST
  里做,mac 走 Overlay 保留原生窗体边;最大化时撤掉,免得误触屏幕边。
- `window_system_menu` 命令(Windows):无边框窗口丢掉的原生右键标题菜单,由
  窗框条右键与左端应用图标点击唤起。不走 WM_NCHITTEST/HTSYSMENU——WebView2
  子窗口铺满客户区,非客户区命中测试到不了 UI 这层。
  > **已知缺口**:同样因为客户区被 WebView2 占满,Windows 11 的 Snap Layouts
  > (悬停最大化键弹平铺菜单)拿不到,需要 `HTMAXBUTTON` 才会触发。这是自绘
  > caption 以来一直如此,不是本次回退。要补得在壳侧挂 WndProc 做
  > `WM_NCCALCSIZE` + `WM_NCHITTEST`(Chrome / Windows Terminal 的做法)。
- lib/contextMenu:命令式右键菜单按指针位定位,dropdown(锚定触发器)表达
  不了;皮相仍取 menu 文档形态(`menu bg-base-100 rounded-box shadow-sm`)。
- FilesDrawer 与 CloudTaskView 详情抽屉:daisyUI drawer 是 checkbox 驱动的
  整页布局原语,与受控开合+拖拽调宽不适配(FilesDrawer 头注 L1-4);面板
  自绘,scrim 用语义色 `bg-base-content/20`。
- xterm/term.css:第三方 canvas/DOM,吃不了主题变量且终端岛恒深色面
  (见 term.css 头注);仅限 xterm 本体,壳外皮相(终端卡)不豁免。
