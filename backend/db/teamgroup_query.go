// Code generated by ent, DO NOT EDIT.

package db

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/chaitin/MonkeyCode/backend/db/host"
	"github.com/chaitin/MonkeyCode/backend/db/image"
	"github.com/chaitin/MonkeyCode/backend/db/mcpupstream"
	"github.com/chaitin/MonkeyCode/backend/db/model"
	"github.com/chaitin/MonkeyCode/backend/db/predicate"
	"github.com/chaitin/MonkeyCode/backend/db/team"
	"github.com/chaitin/MonkeyCode/backend/db/teamgroup"
	"github.com/chaitin/MonkeyCode/backend/db/teamgrouphost"
	"github.com/chaitin/MonkeyCode/backend/db/teamgroupimage"
	"github.com/chaitin/MonkeyCode/backend/db/teamgroupmcpupstream"
	"github.com/chaitin/MonkeyCode/backend/db/teamgroupmember"
	"github.com/chaitin/MonkeyCode/backend/db/teamgroupmodel"
	"github.com/chaitin/MonkeyCode/backend/db/user"
	"github.com/google/uuid"
)

// TeamGroupQuery is the builder for querying TeamGroup entities.
type TeamGroupQuery struct {
	config
	ctx                       *QueryContext
	order                     []teamgroup.OrderOption
	inters                    []Interceptor
	predicates                []predicate.TeamGroup
	withMembers               *UserQuery
	withTeam                  *TeamQuery
	withModels                *ModelQuery
	withImages                *ImageQuery
	withHosts                 *HostQuery
	withMcpUpstreams          *MCPUpstreamQuery
	withTeamGroupMembers      *TeamGroupMemberQuery
	withTeamGroupModels       *TeamGroupModelQuery
	withTeamGroupImages       *TeamGroupImageQuery
	withTeamGroupHosts        *TeamGroupHostQuery
	withTeamGroupMcpUpstreams *TeamGroupMCPUpstreamQuery
	modifiers                 []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TeamGroupQuery builder.
func (_q *TeamGroupQuery) Where(ps ...predicate.TeamGroup) *TeamGroupQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *TeamGroupQuery) Limit(limit int) *TeamGroupQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *TeamGroupQuery) Offset(offset int) *TeamGroupQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *TeamGroupQuery) Unique(unique bool) *TeamGroupQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *TeamGroupQuery) Order(o ...teamgroup.OrderOption) *TeamGroupQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryMembers chains the current query on the "members" edge.
func (_q *TeamGroupQuery) QueryMembers() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(teamgroup.Table, teamgroup.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, teamgroup.MembersTable, teamgroup.MembersPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTeam chains the current query on the "team" edge.
func (_q *TeamGroupQuery) QueryTeam() *TeamQuery {
	query := (&TeamClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(teamgroup.Table, teamgroup.FieldID, selector),
			sqlgraph.To(team.Table, team.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, teamgroup.TeamTable, teamgroup.TeamColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryModels chains the current query on the "models" edge.
func (_q *TeamGroupQuery) QueryModels() *ModelQuery {
	query := (&ModelClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(teamgroup.Table, teamgroup.FieldID, selector),
			sqlgraph.To(model.Table, model.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, teamgroup.ModelsTable, teamgroup.ModelsPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryImages chains the current query on the "images" edge.
func (_q *TeamGroupQuery) QueryImages() *ImageQuery {
	query := (&ImageClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(teamgroup.Table, teamgroup.FieldID, selector),
			sqlgraph.To(image.Table, image.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, teamgroup.ImagesTable, teamgroup.ImagesPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryHosts chains the current query on the "hosts" edge.
func (_q *TeamGroupQuery) QueryHosts() *HostQuery {
	query := (&HostClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(teamgroup.Table, teamgroup.FieldID, selector),
			sqlgraph.To(host.Table, host.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, teamgroup.HostsTable, teamgroup.HostsPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMcpUpstreams chains the current query on the "mcp_upstreams" edge.
func (_q *TeamGroupQuery) QueryMcpUpstreams() *MCPUpstreamQuery {
	query := (&MCPUpstreamClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(teamgroup.Table, teamgroup.FieldID, selector),
			sqlgraph.To(mcpupstream.Table, mcpupstream.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, teamgroup.McpUpstreamsTable, teamgroup.McpUpstreamsPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTeamGroupMembers chains the current query on the "team_group_members" edge.
func (_q *TeamGroupQuery) QueryTeamGroupMembers() *TeamGroupMemberQuery {
	query := (&TeamGroupMemberClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(teamgroup.Table, teamgroup.FieldID, selector),
			sqlgraph.To(teamgroupmember.Table, teamgroupmember.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, teamgroup.TeamGroupMembersTable, teamgroup.TeamGroupMembersColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTeamGroupModels chains the current query on the "team_group_models" edge.
func (_q *TeamGroupQuery) QueryTeamGroupModels() *TeamGroupModelQuery {
	query := (&TeamGroupModelClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(teamgroup.Table, teamgroup.FieldID, selector),
			sqlgraph.To(teamgroupmodel.Table, teamgroupmodel.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, teamgroup.TeamGroupModelsTable, teamgroup.TeamGroupModelsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTeamGroupImages chains the current query on the "team_group_images" edge.
func (_q *TeamGroupQuery) QueryTeamGroupImages() *TeamGroupImageQuery {
	query := (&TeamGroupImageClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(teamgroup.Table, teamgroup.FieldID, selector),
			sqlgraph.To(teamgroupimage.Table, teamgroupimage.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, teamgroup.TeamGroupImagesTable, teamgroup.TeamGroupImagesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTeamGroupHosts chains the current query on the "team_group_hosts" edge.
func (_q *TeamGroupQuery) QueryTeamGroupHosts() *TeamGroupHostQuery {
	query := (&TeamGroupHostClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(teamgroup.Table, teamgroup.FieldID, selector),
			sqlgraph.To(teamgrouphost.Table, teamgrouphost.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, teamgroup.TeamGroupHostsTable, teamgroup.TeamGroupHostsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTeamGroupMcpUpstreams chains the current query on the "team_group_mcp_upstreams" edge.
func (_q *TeamGroupQuery) QueryTeamGroupMcpUpstreams() *TeamGroupMCPUpstreamQuery {
	query := (&TeamGroupMCPUpstreamClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(teamgroup.Table, teamgroup.FieldID, selector),
			sqlgraph.To(teamgroupmcpupstream.Table, teamgroupmcpupstream.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, teamgroup.TeamGroupMcpUpstreamsTable, teamgroup.TeamGroupMcpUpstreamsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first TeamGroup entity from the query.
// Returns a *NotFoundError when no TeamGroup was found.
func (_q *TeamGroupQuery) First(ctx context.Context) (*TeamGroup, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{teamgroup.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *TeamGroupQuery) FirstX(ctx context.Context) *TeamGroup {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TeamGroup ID from the query.
// Returns a *NotFoundError when no TeamGroup ID was found.
func (_q *TeamGroupQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{teamgroup.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *TeamGroupQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TeamGroup entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TeamGroup entity is found.
// Returns a *NotFoundError when no TeamGroup entities are found.
func (_q *TeamGroupQuery) Only(ctx context.Context) (*TeamGroup, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{teamgroup.Label}
	default:
		return nil, &NotSingularError{teamgroup.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *TeamGroupQuery) OnlyX(ctx context.Context) *TeamGroup {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TeamGroup ID in the query.
// Returns a *NotSingularError when more than one TeamGroup ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *TeamGroupQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{teamgroup.Label}
	default:
		err = &NotSingularError{teamgroup.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *TeamGroupQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TeamGroups.
func (_q *TeamGroupQuery) All(ctx context.Context) ([]*TeamGroup, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TeamGroup, *TeamGroupQuery]()
	return withInterceptors[[]*TeamGroup](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *TeamGroupQuery) AllX(ctx context.Context) []*TeamGroup {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TeamGroup IDs.
func (_q *TeamGroupQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(teamgroup.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *TeamGroupQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *TeamGroupQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*TeamGroupQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *TeamGroupQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *TeamGroupQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("db: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *TeamGroupQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TeamGroupQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *TeamGroupQuery) Clone() *TeamGroupQuery {
	if _q == nil {
		return nil
	}
	return &TeamGroupQuery{
		config:                    _q.config,
		ctx:                       _q.ctx.Clone(),
		order:                     append([]teamgroup.OrderOption{}, _q.order...),
		inters:                    append([]Interceptor{}, _q.inters...),
		predicates:                append([]predicate.TeamGroup{}, _q.predicates...),
		withMembers:               _q.withMembers.Clone(),
		withTeam:                  _q.withTeam.Clone(),
		withModels:                _q.withModels.Clone(),
		withImages:                _q.withImages.Clone(),
		withHosts:                 _q.withHosts.Clone(),
		withMcpUpstreams:          _q.withMcpUpstreams.Clone(),
		withTeamGroupMembers:      _q.withTeamGroupMembers.Clone(),
		withTeamGroupModels:       _q.withTeamGroupModels.Clone(),
		withTeamGroupImages:       _q.withTeamGroupImages.Clone(),
		withTeamGroupHosts:        _q.withTeamGroupHosts.Clone(),
		withTeamGroupMcpUpstreams: _q.withTeamGroupMcpUpstreams.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithMembers tells the query-builder to eager-load the nodes that are connected to
// the "members" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TeamGroupQuery) WithMembers(opts ...func(*UserQuery)) *TeamGroupQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMembers = query
	return _q
}

// WithTeam tells the query-builder to eager-load the nodes that are connected to
// the "team" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TeamGroupQuery) WithTeam(opts ...func(*TeamQuery)) *TeamGroupQuery {
	query := (&TeamClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTeam = query
	return _q
}

// WithModels tells the query-builder to eager-load the nodes that are connected to
// the "models" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TeamGroupQuery) WithModels(opts ...func(*ModelQuery)) *TeamGroupQuery {
	query := (&ModelClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withModels = query
	return _q
}

// WithImages tells the query-builder to eager-load the nodes that are connected to
// the "images" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TeamGroupQuery) WithImages(opts ...func(*ImageQuery)) *TeamGroupQuery {
	query := (&ImageClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withImages = query
	return _q
}

// WithHosts tells the query-builder to eager-load the nodes that are connected to
// the "hosts" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TeamGroupQuery) WithHosts(opts ...func(*HostQuery)) *TeamGroupQuery {
	query := (&HostClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withHosts = query
	return _q
}

// WithMcpUpstreams tells the query-builder to eager-load the nodes that are connected to
// the "mcp_upstreams" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TeamGroupQuery) WithMcpUpstreams(opts ...func(*MCPUpstreamQuery)) *TeamGroupQuery {
	query := (&MCPUpstreamClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMcpUpstreams = query
	return _q
}

// WithTeamGroupMembers tells the query-builder to eager-load the nodes that are connected to
// the "team_group_members" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TeamGroupQuery) WithTeamGroupMembers(opts ...func(*TeamGroupMemberQuery)) *TeamGroupQuery {
	query := (&TeamGroupMemberClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTeamGroupMembers = query
	return _q
}

// WithTeamGroupModels tells the query-builder to eager-load the nodes that are connected to
// the "team_group_models" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TeamGroupQuery) WithTeamGroupModels(opts ...func(*TeamGroupModelQuery)) *TeamGroupQuery {
	query := (&TeamGroupModelClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTeamGroupModels = query
	return _q
}

// WithTeamGroupImages tells the query-builder to eager-load the nodes that are connected to
// the "team_group_images" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TeamGroupQuery) WithTeamGroupImages(opts ...func(*TeamGroupImageQuery)) *TeamGroupQuery {
	query := (&TeamGroupImageClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTeamGroupImages = query
	return _q
}

// WithTeamGroupHosts tells the query-builder to eager-load the nodes that are connected to
// the "team_group_hosts" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TeamGroupQuery) WithTeamGroupHosts(opts ...func(*TeamGroupHostQuery)) *TeamGroupQuery {
	query := (&TeamGroupHostClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTeamGroupHosts = query
	return _q
}

// WithTeamGroupMcpUpstreams tells the query-builder to eager-load the nodes that are connected to
// the "team_group_mcp_upstreams" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TeamGroupQuery) WithTeamGroupMcpUpstreams(opts ...func(*TeamGroupMCPUpstreamQuery)) *TeamGroupQuery {
	query := (&TeamGroupMCPUpstreamClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTeamGroupMcpUpstreams = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		DeletedAt time.Time `json:"deleted_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TeamGroup.Query().
//		GroupBy(teamgroup.FieldDeletedAt).
//		Aggregate(db.Count()).
//		Scan(ctx, &v)
func (_q *TeamGroupQuery) GroupBy(field string, fields ...string) *TeamGroupGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TeamGroupGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = teamgroup.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		DeletedAt time.Time `json:"deleted_at,omitempty"`
//	}
//
//	client.TeamGroup.Query().
//		Select(teamgroup.FieldDeletedAt).
//		Scan(ctx, &v)
func (_q *TeamGroupQuery) Select(fields ...string) *TeamGroupSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &TeamGroupSelect{TeamGroupQuery: _q}
	sbuild.label = teamgroup.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TeamGroupSelect configured with the given aggregations.
func (_q *TeamGroupQuery) Aggregate(fns ...AggregateFunc) *TeamGroupSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *TeamGroupQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("db: uninitialized interceptor (forgotten import db/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !teamgroup.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("db: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *TeamGroupQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TeamGroup, error) {
	var (
		nodes       = []*TeamGroup{}
		_spec       = _q.querySpec()
		loadedTypes = [11]bool{
			_q.withMembers != nil,
			_q.withTeam != nil,
			_q.withModels != nil,
			_q.withImages != nil,
			_q.withHosts != nil,
			_q.withMcpUpstreams != nil,
			_q.withTeamGroupMembers != nil,
			_q.withTeamGroupModels != nil,
			_q.withTeamGroupImages != nil,
			_q.withTeamGroupHosts != nil,
			_q.withTeamGroupMcpUpstreams != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TeamGroup).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TeamGroup{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withMembers; query != nil {
		if err := _q.loadMembers(ctx, query, nodes,
			func(n *TeamGroup) { n.Edges.Members = []*User{} },
			func(n *TeamGroup, e *User) { n.Edges.Members = append(n.Edges.Members, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTeam; query != nil {
		if err := _q.loadTeam(ctx, query, nodes, nil,
			func(n *TeamGroup, e *Team) { n.Edges.Team = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withModels; query != nil {
		if err := _q.loadModels(ctx, query, nodes,
			func(n *TeamGroup) { n.Edges.Models = []*Model{} },
			func(n *TeamGroup, e *Model) { n.Edges.Models = append(n.Edges.Models, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withImages; query != nil {
		if err := _q.loadImages(ctx, query, nodes,
			func(n *TeamGroup) { n.Edges.Images = []*Image{} },
			func(n *TeamGroup, e *Image) { n.Edges.Images = append(n.Edges.Images, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withHosts; query != nil {
		if err := _q.loadHosts(ctx, query, nodes,
			func(n *TeamGroup) { n.Edges.Hosts = []*Host{} },
			func(n *TeamGroup, e *Host) { n.Edges.Hosts = append(n.Edges.Hosts, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withMcpUpstreams; query != nil {
		if err := _q.loadMcpUpstreams(ctx, query, nodes,
			func(n *TeamGroup) { n.Edges.McpUpstreams = []*MCPUpstream{} },
			func(n *TeamGroup, e *MCPUpstream) { n.Edges.McpUpstreams = append(n.Edges.McpUpstreams, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTeamGroupMembers; query != nil {
		if err := _q.loadTeamGroupMembers(ctx, query, nodes,
			func(n *TeamGroup) { n.Edges.TeamGroupMembers = []*TeamGroupMember{} },
			func(n *TeamGroup, e *TeamGroupMember) { n.Edges.TeamGroupMembers = append(n.Edges.TeamGroupMembers, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTeamGroupModels; query != nil {
		if err := _q.loadTeamGroupModels(ctx, query, nodes,
			func(n *TeamGroup) { n.Edges.TeamGroupModels = []*TeamGroupModel{} },
			func(n *TeamGroup, e *TeamGroupModel) { n.Edges.TeamGroupModels = append(n.Edges.TeamGroupModels, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTeamGroupImages; query != nil {
		if err := _q.loadTeamGroupImages(ctx, query, nodes,
			func(n *TeamGroup) { n.Edges.TeamGroupImages = []*TeamGroupImage{} },
			func(n *TeamGroup, e *TeamGroupImage) { n.Edges.TeamGroupImages = append(n.Edges.TeamGroupImages, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTeamGroupHosts; query != nil {
		if err := _q.loadTeamGroupHosts(ctx, query, nodes,
			func(n *TeamGroup) { n.Edges.TeamGroupHosts = []*TeamGroupHost{} },
			func(n *TeamGroup, e *TeamGroupHost) { n.Edges.TeamGroupHosts = append(n.Edges.TeamGroupHosts, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTeamGroupMcpUpstreams; query != nil {
		if err := _q.loadTeamGroupMcpUpstreams(ctx, query, nodes,
			func(n *TeamGroup) { n.Edges.TeamGroupMcpUpstreams = []*TeamGroupMCPUpstream{} },
			func(n *TeamGroup, e *TeamGroupMCPUpstream) {
				n.Edges.TeamGroupMcpUpstreams = append(n.Edges.TeamGroupMcpUpstreams, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *TeamGroupQuery) loadMembers(ctx context.Context, query *UserQuery, nodes []*TeamGroup, init func(*TeamGroup), assign func(*TeamGroup, *User)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[uuid.UUID]*TeamGroup)
	nids := make(map[uuid.UUID]map[*TeamGroup]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(teamgroup.MembersTable)
		s.Join(joinT).On(s.C(user.FieldID), joinT.C(teamgroup.MembersPrimaryKey[0]))
		s.Where(sql.InValues(joinT.C(teamgroup.MembersPrimaryKey[1]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(teamgroup.MembersPrimaryKey[1]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(uuid.UUID)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := *values[0].(*uuid.UUID)
				inValue := *values[1].(*uuid.UUID)
				if nids[inValue] == nil {
					nids[inValue] = map[*TeamGroup]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*User](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "members" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (_q *TeamGroupQuery) loadTeam(ctx context.Context, query *TeamQuery, nodes []*TeamGroup, init func(*TeamGroup), assign func(*TeamGroup, *Team)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*TeamGroup)
	for i := range nodes {
		fk := nodes[i].TeamID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(team.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "team_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *TeamGroupQuery) loadModels(ctx context.Context, query *ModelQuery, nodes []*TeamGroup, init func(*TeamGroup), assign func(*TeamGroup, *Model)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[uuid.UUID]*TeamGroup)
	nids := make(map[uuid.UUID]map[*TeamGroup]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(teamgroup.ModelsTable)
		s.Join(joinT).On(s.C(model.FieldID), joinT.C(teamgroup.ModelsPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(teamgroup.ModelsPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(teamgroup.ModelsPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(uuid.UUID)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := *values[0].(*uuid.UUID)
				inValue := *values[1].(*uuid.UUID)
				if nids[inValue] == nil {
					nids[inValue] = map[*TeamGroup]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Model](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "models" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (_q *TeamGroupQuery) loadImages(ctx context.Context, query *ImageQuery, nodes []*TeamGroup, init func(*TeamGroup), assign func(*TeamGroup, *Image)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[uuid.UUID]*TeamGroup)
	nids := make(map[uuid.UUID]map[*TeamGroup]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(teamgroup.ImagesTable)
		s.Join(joinT).On(s.C(image.FieldID), joinT.C(teamgroup.ImagesPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(teamgroup.ImagesPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(teamgroup.ImagesPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(uuid.UUID)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := *values[0].(*uuid.UUID)
				inValue := *values[1].(*uuid.UUID)
				if nids[inValue] == nil {
					nids[inValue] = map[*TeamGroup]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Image](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "images" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (_q *TeamGroupQuery) loadHosts(ctx context.Context, query *HostQuery, nodes []*TeamGroup, init func(*TeamGroup), assign func(*TeamGroup, *Host)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[uuid.UUID]*TeamGroup)
	nids := make(map[string]map[*TeamGroup]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(teamgroup.HostsTable)
		s.Join(joinT).On(s.C(host.FieldID), joinT.C(teamgroup.HostsPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(teamgroup.HostsPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(teamgroup.HostsPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(uuid.UUID)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := *values[0].(*uuid.UUID)
				inValue := values[1].(*sql.NullString).String
				if nids[inValue] == nil {
					nids[inValue] = map[*TeamGroup]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Host](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "hosts" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (_q *TeamGroupQuery) loadMcpUpstreams(ctx context.Context, query *MCPUpstreamQuery, nodes []*TeamGroup, init func(*TeamGroup), assign func(*TeamGroup, *MCPUpstream)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[uuid.UUID]*TeamGroup)
	nids := make(map[uuid.UUID]map[*TeamGroup]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(teamgroup.McpUpstreamsTable)
		s.Join(joinT).On(s.C(mcpupstream.FieldID), joinT.C(teamgroup.McpUpstreamsPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(teamgroup.McpUpstreamsPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(teamgroup.McpUpstreamsPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(uuid.UUID)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := *values[0].(*uuid.UUID)
				inValue := *values[1].(*uuid.UUID)
				if nids[inValue] == nil {
					nids[inValue] = map[*TeamGroup]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*MCPUpstream](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "mcp_upstreams" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (_q *TeamGroupQuery) loadTeamGroupMembers(ctx context.Context, query *TeamGroupMemberQuery, nodes []*TeamGroup, init func(*TeamGroup), assign func(*TeamGroup, *TeamGroupMember)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*TeamGroup)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(teamgroupmember.FieldGroupID)
	}
	query.Where(predicate.TeamGroupMember(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(teamgroup.TeamGroupMembersColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.GroupID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "group_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *TeamGroupQuery) loadTeamGroupModels(ctx context.Context, query *TeamGroupModelQuery, nodes []*TeamGroup, init func(*TeamGroup), assign func(*TeamGroup, *TeamGroupModel)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*TeamGroup)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(teamgroupmodel.FieldGroupID)
	}
	query.Where(predicate.TeamGroupModel(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(teamgroup.TeamGroupModelsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.GroupID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "group_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *TeamGroupQuery) loadTeamGroupImages(ctx context.Context, query *TeamGroupImageQuery, nodes []*TeamGroup, init func(*TeamGroup), assign func(*TeamGroup, *TeamGroupImage)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*TeamGroup)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(teamgroupimage.FieldGroupID)
	}
	query.Where(predicate.TeamGroupImage(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(teamgroup.TeamGroupImagesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.GroupID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "group_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *TeamGroupQuery) loadTeamGroupHosts(ctx context.Context, query *TeamGroupHostQuery, nodes []*TeamGroup, init func(*TeamGroup), assign func(*TeamGroup, *TeamGroupHost)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*TeamGroup)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(teamgrouphost.FieldGroupID)
	}
	query.Where(predicate.TeamGroupHost(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(teamgroup.TeamGroupHostsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.GroupID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "group_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *TeamGroupQuery) loadTeamGroupMcpUpstreams(ctx context.Context, query *TeamGroupMCPUpstreamQuery, nodes []*TeamGroup, init func(*TeamGroup), assign func(*TeamGroup, *TeamGroupMCPUpstream)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*TeamGroup)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(teamgroupmcpupstream.FieldGroupID)
	}
	query.Where(predicate.TeamGroupMCPUpstream(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(teamgroup.TeamGroupMcpUpstreamsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.GroupID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "group_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *TeamGroupQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *TeamGroupQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(teamgroup.Table, teamgroup.Columns, sqlgraph.NewFieldSpec(teamgroup.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, teamgroup.FieldID)
		for i := range fields {
			if fields[i] != teamgroup.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withTeam != nil {
			_spec.Node.AddColumnOnce(teamgroup.FieldTeamID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *TeamGroupQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(teamgroup.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = teamgroup.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *TeamGroupQuery) ForUpdate(opts ...sql.LockOption) *TeamGroupQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *TeamGroupQuery) ForShare(opts ...sql.LockOption) *TeamGroupQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *TeamGroupQuery) Modify(modifiers ...func(s *sql.Selector)) *TeamGroupSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// TeamGroupGroupBy is the group-by builder for TeamGroup entities.
type TeamGroupGroupBy struct {
	selector
	build *TeamGroupQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *TeamGroupGroupBy) Aggregate(fns ...AggregateFunc) *TeamGroupGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *TeamGroupGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TeamGroupQuery, *TeamGroupGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *TeamGroupGroupBy) sqlScan(ctx context.Context, root *TeamGroupQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TeamGroupSelect is the builder for selecting fields of TeamGroup entities.
type TeamGroupSelect struct {
	*TeamGroupQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *TeamGroupSelect) Aggregate(fns ...AggregateFunc) *TeamGroupSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *TeamGroupSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TeamGroupQuery, *TeamGroupSelect](ctx, _s.TeamGroupQuery, _s, _s.inters, v)
}

func (_s *TeamGroupSelect) sqlScan(ctx context.Context, root *TeamGroupQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *TeamGroupSelect) Modify(modifiers ...func(s *sql.Selector)) *TeamGroupSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
