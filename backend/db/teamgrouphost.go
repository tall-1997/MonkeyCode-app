// Code generated by ent, DO NOT EDIT.

package db

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/chaitin/MonkeyCode/backend/db/host"
	"github.com/chaitin/MonkeyCode/backend/db/teamgroup"
	"github.com/chaitin/MonkeyCode/backend/db/teamgrouphost"
	"github.com/google/uuid"
)

// TeamGroupHost is the model entity for the TeamGroupHost schema.
type TeamGroupHost struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// GroupID holds the value of the "group_id" field.
	GroupID uuid.UUID `json:"group_id,omitempty"`
	// HostID holds the value of the "host_id" field.
	HostID string `json:"host_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TeamGroupHostQuery when eager-loading is set.
	Edges        TeamGroupHostEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TeamGroupHostEdges holds the relations/edges for other nodes in the graph.
type TeamGroupHostEdges struct {
	// Group holds the value of the group edge.
	Group *TeamGroup `json:"group,omitempty"`
	// Host holds the value of the host edge.
	Host *Host `json:"host,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// GroupOrErr returns the Group value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TeamGroupHostEdges) GroupOrErr() (*TeamGroup, error) {
	if e.Group != nil {
		return e.Group, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: teamgroup.Label}
	}
	return nil, &NotLoadedError{edge: "group"}
}

// HostOrErr returns the Host value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TeamGroupHostEdges) HostOrErr() (*Host, error) {
	if e.Host != nil {
		return e.Host, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: host.Label}
	}
	return nil, &NotLoadedError{edge: "host"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TeamGroupHost) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case teamgrouphost.FieldHostID:
			values[i] = new(sql.NullString)
		case teamgrouphost.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case teamgrouphost.FieldID, teamgrouphost.FieldGroupID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TeamGroupHost fields.
func (_m *TeamGroupHost) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case teamgrouphost.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case teamgrouphost.FieldGroupID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field group_id", values[i])
			} else if value != nil {
				_m.GroupID = *value
			}
		case teamgrouphost.FieldHostID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field host_id", values[i])
			} else if value.Valid {
				_m.HostID = value.String
			}
		case teamgrouphost.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TeamGroupHost.
// This includes values selected through modifiers, order, etc.
func (_m *TeamGroupHost) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryGroup queries the "group" edge of the TeamGroupHost entity.
func (_m *TeamGroupHost) QueryGroup() *TeamGroupQuery {
	return NewTeamGroupHostClient(_m.config).QueryGroup(_m)
}

// QueryHost queries the "host" edge of the TeamGroupHost entity.
func (_m *TeamGroupHost) QueryHost() *HostQuery {
	return NewTeamGroupHostClient(_m.config).QueryHost(_m)
}

// Update returns a builder for updating this TeamGroupHost.
// Note that you need to call TeamGroupHost.Unwrap() before calling this method if this TeamGroupHost
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *TeamGroupHost) Update() *TeamGroupHostUpdateOne {
	return NewTeamGroupHostClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the TeamGroupHost entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *TeamGroupHost) Unwrap() *TeamGroupHost {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("db: TeamGroupHost is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *TeamGroupHost) String() string {
	var builder strings.Builder
	builder.WriteString("TeamGroupHost(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("group_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.GroupID))
	builder.WriteString(", ")
	builder.WriteString("host_id=")
	builder.WriteString(_m.HostID)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// TeamGroupHosts is a parsable slice of TeamGroupHost.
type TeamGroupHosts []*TeamGroupHost
