# 需求实施计划

- [x] 1. 实现安全的背景资产服务
  - [x] 1.1 新增 Rust 背景资产模块与托管元数据
    - 在应用私有数据目录中实现版本化元数据、内容散列文件名和原子替换
    - 限制读取范围为背景资产目录，拒绝路径逃逸和未知元数据版本
    - 对应需求 1.2、1.3、1.6、4.4、6.1
  - [x] 1.2 实现背景图片导入、读取和清除命令
    - 校验普通文件、20 MiB、PNG/JPEG/WebP 实际格式、解码尺寸、16,384 px 边长和 50MP 像素上限
    - 返回受控 data URL，并确保失败时旧背景保持可用
    - 将命令注册到 Tauri handler 与主窗口 capability
    - 对应需求 1.1–1.6、4.2、4.4、6.4
  - [x] 1.3 编写 Rust 资产服务测试
    - 覆盖支持格式、伪装格式、损坏文件、大小/尺寸限制、原子替换、路径封闭和幂等清除
    - 验证正确性属性 3、4
    - 对应需求 1.5、1.6、4.4、6.5

- [x] 2. 实现背景偏好与桌面 IPC
  - [x] 2.1 新增前端背景 IPC 封装
    - 封装原生单文件选择、导入、读取和清除命令，浏览器模式安全降级
    - 对应需求 1.1、1.2、1.4、6.1
  - [x] 2.2 新增版本化背景偏好和运行时初始化
    - 实现不透明度、模糊度、填充方式的校验、持久化和 DOM CSS 变量应用
    - 在 React 挂载前读取并预解码托管图片，失败时保留主题后备底色
    - 对应需求 3.1–3.6、4.1–4.4、6.2、6.3
  - [x] 2.3 编写偏好与 IPC 单元测试
    - 覆盖脏值回退、参数边界、填充映射、初始化成功/失败、对话框取消和浏览器降级
    - 验证正确性属性 5、7、8
    - 对应需求 3.1–3.6、4.2、4.4、6.3、6.5

- [x] 3. 接入外观设置与工作台透明表面
  - [x] 3.1 实现外观设置中的背景编辑器
    - 添加预览、选择/更换、清除、35%–100% 不透明度、0–20 px 模糊和覆盖/适应/平铺控件
    - 增加低不透明度提示、错误外显、控件禁用和中英文文案
    - 确保背景设置即时生效且不进入引擎配置保存条
    - 对应需求 1.1、1.4–1.6、3.1–3.6、5.1、6.2
  - [x] 3.2 增加唯一工作台背景层和语义表面类
    - 让任务列、分格画布、pane、Chat 和 Cloud 工作台表面使用单层可调主题覆盖
    - 保持设置页、菜单、弹窗、卡片、终端和关键交互表面不透明
    - 未配置背景时保持现有主题外观不变
    - 对应需求 2.1–2.6、4.3、4.5、5.2
  - [x] 3.3 实现系统可访问性降级
    - 在减少透明度或减少动态效果偏好下强制实色表面并关闭模糊，不修改持久化值
    - 对应需求 5.3、5.4
  - [x] 3.4 编写设置与工作台组件测试
    - 覆盖成功/失败导入、清除、即时控件、低不透明度提示、唯一背景层、pane 单层透明和设置页实色
    - 验证正确性属性 1、2、6、8
    - 对应需求 1.4–1.6、2.1–2.6、3.1–3.6、5.1–5.4、6.5

- [x] 4. 检查点
  - [x] 4.1 确保所有测试通过,如有疑问请询问用户
    - 运行 Rust 背景模块测试、前端聚焦测试和 TypeScript 类型检查
    - 修复由本功能引入的失败并记录无法执行的验证项

- [x] 5. 高强度 review 修复
  - [x] 5.1 兼容旧 WKWebView 并移出主线程重活
    - 启动入口不使用顶层 await，仍在 React 首次挂载前收敛背景初始化
    - Tauri 背景命令改 async，并以 spawn_blocking 承载文件 I/O、图片解码和编码
  - [x] 5.2 实现可确认、可丢弃的背景资产事务
    - import 只写 staged 资产，WebView 预解码成功后 confirm 才切换 current
    - 跨组件 generation 与串行队列保证 last action wins，解码失败/过期动作 discard
  - [x] 5.3 加固并发清理与文件读取
    - 使用跨进程文件锁，清理从锁内 current/pending 推导保留集
    - 使用 MAX_BYTES+1 有界读取；等值 symlink 托管资产强制原子重写
  - [x] 5.4 修复 contain 裁边与启动错误本地化
    - 外扩层以 padding/content-box 保持 contain 按工作台原尺寸计算
    - 启动错误发布结构化 code/detail，由 SettingsView i18n 翻译 UI 前缀
  - [x] 5.5 补齐聚焦回归并执行 build/command contract
